# 01_clean_spotrac.R
# Will parse and standardize the Spotrac raw HTML into a flat transaction table with
# fields for player, position, signing team, previous team, contract AAV, contract years,
# UFA/RFA type, and signing year.
