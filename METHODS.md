# Full Methods Appendix

This appendix documents the complete Version 1 workflow for the NHL free-agency study and is intended to be reproducible from raw extracts through model outputs.

## 1) Data sources and extraction code

Primary extraction scripts:

- `R/01_data_extraction/01_spotrac_pull.R`
- `R/01_data_extraction/02_nhlscraper_pull.R`

Raw source files currently used by the pipeline:

- `data/raw/spotrac/nhl_signed_free_agents_raw.csv`
- `data/raw/nhlscraper/nhl_team_season_performance_raw.csv`

Processed inputs generated by cleaning and feature engineering:

- `data/processed/nhl_signed_free_agents_clean.csv`
- `data/processed/nhl_team_season_performance_clean.csv`
- `data/processed/nhl_team_mis_scores.csv`
- `data/processed/nhl_team_cap_variables.csv`
- `data/processed/nhl_team_geography_variables.csv`
- `data/processed/nhl_team_net_aav.csv`
- `data/processed/nhl_team_contract_length.csv`
- `data/processed/nhl_team_retention.csv`
- `data/processed/nhl_master_analysis_panel.csv`

## 2) Scope and exclusion rules

Version 1 scope includes unrestricted free-agent (UFA) signings only.

Explicit exclusions:

- Restricted free agents (RFAs)
- Minor-league contract rows
- Same-team re-signings when constructing movement-event features
- Trade-deadline acquisitions
- AHL call-ups
- Entry-level signings

Operational filters are implemented in:

- `R/02_data_cleaning/01_clean_spotrac.R`
- `R/03_feature_engineering/01_build_MIS.R`

## 3) Variable definitions and formulas

### 3.1 Unit of analysis

- Signing-level table for transaction engineering
- Team-season panel for inferential models

Final model sample:

- 252 team-season rows for seasons 2018 through 2025

### 3.2 Outcome variable

The primary dependent variable is season-over-season team change:

- `points_pct_change = points_percentage - prior_season_points_pct`

Implemented in:

- `R/04_analysis/02_regression_models.R`

### 3.3 Movement Impact Score (MIS)

MIS is a position-weighted aggregate of offseason UFA spending:

- `weighted_aav_i = aav_i * tier_weight_i`
- `total_mis_team_season = sum(weighted_aav_i over signings i)`

Version 1 tier mapping and weights:

- Tier 1 (G): 0.35
- Tier 2 (D): 0.25
- Tier 3 (C): 0.20
- Tier 4 (LW/RW): 0.15
- Tier 5 (other/unmatched): 0.05

Implemented in:

- `R/03_feature_engineering/01_build_MIS.R`

### 3.4 Contract value normalization

Contract fields are normalized as numeric currency values during cleaning.

- `contract_value` and `aav` are parsed from string currency fields
- If `aav` is missing or zero and contract value and years exist:
  - `aav = contract_value / contract_years`
  - row is flagged with `aav_imputed = TRUE`
- Rows with non-usable AAV are retained with `data_quality_note` for transparent filtering in downstream steps

Implemented in:

- `R/02_data_cleaning/01_clean_spotrac.R`

### 3.5 Cap and spending variables

Cap-related variables include:

- `cap_ceiling` from year lookup
- `cap_ceiling_change = cap_ceiling_t - cap_ceiling_(t-1)`
- `cap_spending_pct = total_aav_spent / cap_ceiling`
- `heavy_spender = cap_spending_pct > 0.15`

Implemented in:

- `R/03_feature_engineering/02_build_cap_variables.R`

### 3.6 Prior-finish control

The prior-season team strength control is:

- `prior_season_points_pct`

Construction details:

- Prior-season points percentage is joined by team and prior year
- Utah franchise continuity bridge uses ARI prior-year performance for UTA rows
- Expansion/no-prior rows are filled with prior-year league mean

Implemented in:

- `R/03_feature_engineering/04_build_master_panel.R`

### 3.7 V1.1 UFA-side variables

Version 1.1 adds three methodological completion variables using existing UFA data.

Net spending variable:

- `net_aav = total_aav_committed - total_aav_lost`
- `total_aav_committed`: grouped sum of `aav` by `signing_team`, `spotrac_year`
- `total_aav_lost`: grouped sum of `aav` by `previous_team`, `spotrac_year` (excluding `UNKNOWN` previous team)

Contract-length variables:

- `mean_contract_years`
- `max_contract_years`
- `count_long_term` where `contract_years >= 5`
- `weighted_avg_years = sum(contract_years * aav) / sum(aav)`

Retention strategy variables:

- `total_ufa_decisions`
- `count_re_signed`
- `count_new_signings`
- `pct_retained = count_re_signed / total_ufa_decisions`
- `aav_retained`, `aav_new`
- `pct_aav_retained = aav_retained / (aav_retained + aav_new)`
- `no_ufa_activity` flag

Conditional NA rule for retention percentages:

- `pct_retained` and `pct_aav_retained` are NA only when `no_ufa_activity = TRUE`

Implementation scripts:

- `R/03_feature_engineering/05_build_net_aav.R`
- `R/03_feature_engineering/06_build_contract_length.R`
- `R/03_feature_engineering/07_build_retention.R`

Scope-limit note for net spending interpretation:

- Retirements are not captured (no destination signing row)
- Unsigned departures at summer close are not captured
- Trade losses are not captured

## 4) Model specifications and regression tables

Core model script:

- `R/04_analysis/02_regression_models.R`

### 4.1 Full sample models (2018-2025)

Model A (raw spending):

- `points_pct_change ~ total_aav_spent + prior_season_points_pct + season_type + cap_ceiling_change`

Model B (MIS):

- `points_pct_change ~ total_mis + prior_season_points_pct + season_type + cap_ceiling_change`

### 4.2 Restricted sample robustness models (excluding 2020 and 2021)

Model A restricted:

- same as Model A, estimated on non-COVID rows

Model B restricted:

- same as Model B, estimated on non-COVID rows

### 4.3 V1.1 extension models

Model C (net spending in place of raw spending):

- `points_pct_change ~ net_aav + prior_season_points_pct + season_type + cap_ceiling_change`

Model D (contract-length features added to MIS):

- `points_pct_change ~ total_mis + mean_contract_years + count_long_term + prior_season_points_pct + season_type + cap_ceiling_change`

Model E (retention added to MIS):

- `points_pct_change ~ total_mis + pct_retained + prior_season_points_pct + season_type + cap_ceiling_change`

Model F (combined V1.1 model):

- `points_pct_change ~ net_aav + mean_contract_years + pct_retained + prior_season_points_pct + season_type + cap_ceiling_change`

Each of Models C-F is estimated on:

- full sample (2018-2025)
- restricted sample (excluding 2020 and 2021)

### 4.4 Output regression tables

- `output/tables/model_a_full_coefficients.csv`
- `output/tables/model_b_full_coefficients.csv`
- `output/tables/model_a_restricted_coefficients.csv`
- `output/tables/model_b_restricted_coefficients.csv`
- `output/tables/model_c_full_coefficients.csv`
- `output/tables/model_c_restricted_coefficients.csv`
- `output/tables/model_d_full_coefficients.csv`
- `output/tables/model_d_restricted_coefficients.csv`
- `output/tables/model_e_full_coefficients.csv`
- `output/tables/model_e_restricted_coefficients.csv`
- `output/tables/model_f_full_coefficients.csv`
- `output/tables/model_f_restricted_coefficients.csv`
- `output/tables/model_comparison_summary.csv`

The reported headline coefficient is the prior-finish slope on `prior_season_points_pct` (approximately -0.42, statistically significant across specifications).

V1.1 rerun result summary (May 2026):

- `net_aav` is statistically meaningful in Models C and F (full and restricted)
- contract-length terms are not statistically meaningful
- `pct_retained` is not statistically meaningful in full sample and suggestive in one restricted specification

## 5) Sensitivity analyses

### 5.1 Excluding COVID-affected seasons

Sensitivity implemented in production pipeline:

- Excludes `season_type` values `covid_shortened` and `covid_bubble`
- Re-estimates both spending and MIS models on restricted sample

Implemented in:

- `R/04_analysis/02_regression_models.R`

### 5.2 Alternate MIS weighting sensitivity

Version 1 baseline uses fixed tier weights in `R/03_feature_engineering/01_build_MIS.R`.

To run alternate MIS weighting sensitivity:

1. Edit `tier_weights` in `R/03_feature_engineering/01_build_MIS.R`
2. Re-run the reproducibility script in Section 6
3. Compare updated outputs in:
   - `output/tables/model_b_full_coefficients.csv`
   - `output/tables/model_b_restricted_coefficients.csv`
   - `output/tables/model_comparison_summary.csv`

## 6) Reproducibility: rerun main results

A reproducible runner is provided here:

- `R/06_reproducibility/01_reproduce_main_results.R`

From repository root:

```bash
Rscript R/06_reproducibility/01_reproduce_main_results.R
```

This executes data cleaning, feature engineering, and core analysis scripts in sequence and refreshes the main processed panel and model output tables.

## 7) Audit and QA artifacts

For reproducibility and quality checks, see:

- Dashboard audit output: `output/tables/audit_report.json`
- Spotrac QA summaries:
  - `output/tables/spotrac_raw_qa_year_totals.csv`
  - `output/tables/spotrac_raw_qa_key_duplicates.csv`
  - `output/tables/spotrac_raw_qa_missing_field_counts.csv`

Related docs:

- `docs/methodology/methodology_notes.md`
- `docs/scope/scope_definition.md`
- `docs/audit/non-data-user-audit-checklist.md`

## 8) Limitations

- Correlation does not imply causation.
- UFA spending may be endogenous to front-office expectations, injuries, and latent roster context.
- In-season trades, prospect development, and non-UFA roster channels are not explicitly modeled in Version 1.
- Coefficients are conditional on the current specification and variable construction choices, including MIS weighting assumptions.
